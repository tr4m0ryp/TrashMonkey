21:`thresholds.yaml`. Wiring those artifacts to cameras or control hardware
25:class**: it is a multi-frame consensus rule applied downstream of the
29:(RealWaste) is held out as an unseen test set, and a camera-degraded copy
40:| garbage-det.    |   | (Grounding DINO boxes, pHash dedup,  |   | camera-degrade  |
65:   low-cost camera sensors at train time -- JPEG blocking, ISO noise, motion
66:   blur, white-balance drift, downscaling -- so cheap-camera artifacts are
68:   Colab; every run appends config, git hash, metrics, and runtime to
157:Open `notebooks/manager.ipynb` in Colab and "Runtime > Run all". The notebook
186:downstream system needs; camera capture and decision plumbing are not part
232:domain gap to any specific camera setup is managed (degradation-aware
=LINT=
.venv/bin/ruff check src tests
All checks passed!
.venv/bin/mypy src
Success: no issues found in 97 source files

============================= 271 passed in 11.07s =============================
